import { theme } from 'antd';

const { defaultAlgorithm } = theme;

const customTheme = {
  algorithm: defaultAlgorithm,
  token: {
    colorPrimary: '#007bff',
    colorLink: '#0052C2',
    colorLinkHover: '#4324FF'
  }
};

export default customTheme;
